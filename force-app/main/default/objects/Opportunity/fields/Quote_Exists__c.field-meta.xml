<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Quote_Exists__c</fullName>
    <description>Indicates whether a Primary (Synced) Quote is associated with the Opportunity using the standard SyncedQuoteId field.</description>
    <formula>NOT(ISBLANK(SyncedQuoteId))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Checked when a primary quote is linked to this opportunity. A quote must be synced to be considered.</inlineHelpText>
    <label>Quote Exists</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
